{{ form_start(form) }}
    <div class="mb-3">
        {{  form_row(form.typeOp) }}
    </div>
    <div class="mb-3 mt-4">
        <p class="h4">Operator :</p>
        <div class="row">
            <div class="col-6">
                {{ form_row(form.operator) }}
            </div>
            <div class="col-6">
                <div class="mb-3">
                    <label for="operator-team-balance" class="form-label">Current money balance of the team</label>
                    <input disabled ="text" id="operator-team-balance" class="form-control">
                </div>
            </div>
        </div>
        
    </div>

    <div class="mb-3 mt-4">
        <p class="h4">Concern :</p>
        <div class="row">
            <div class="col-6">
                {{ form_row(form.concern) }}
            </div>
            <div class="col-6">
                <div class="mb-3">
                    <label for="concern-team-balance" class="form-label">Current money balance of the concern</label>
                    <input disabled type="text" id="concern-team-balance" class="form-control">
                </div>
            </div>
        </div>
        
    </div>

    <div class="mb-3 mt-4">
        {{ form_row(form.amount) }}
    </div>

	<button type="submit" class="btn btn-primary">Submit</button>
{{ form_end(form) }}
